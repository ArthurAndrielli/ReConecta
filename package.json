{
  "name": "reconecta",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node src/js/phases/validate-bank.mjs",
    "serve": "python -m http.server 4173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
